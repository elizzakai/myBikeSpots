export default interface BikeParkInput {
  location: string;
  coordinates?: [number, number, number, number];
  // query: string;
  // stolen_location?: string;
  // stolen_coordinates?: [number, number];
  // date_stolen?: number;
  // description?: string | null;
  // url: string;
}
